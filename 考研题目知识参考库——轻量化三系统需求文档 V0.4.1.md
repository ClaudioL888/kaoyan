# 考研题目知识参考库——轻量化三系统需求文档 V0.4.1

版本：V0.4.1

状态：后续章节唯一有效执行标准

取代关系：本文件取代 `考研题目知识参考库——轻量化三系统需求文档 V0.4.md`。V0.4 及更早文档仅用于追溯，不得作为后续章节的生产或验收依据。

定位：以题目为中心、以官方解析和教材核心知识为证据、以题型与方法归纳为组织方式、以用户主动反馈为增量的轻量化考研数学问答知识库。

## 0. 强约束声明

本文件中的“必须”“禁止”“仅当”“不得”均为验收硬约束，不是建议。主 Agent 与所有子 Agent 必须完整阅读本文件后再开始工作，不得自行弱化、改写或用“时间不足”“首轮草稿”“数量已齐”解释性绕过。

### 0.1 识别模型白名单（新增硬约束）

本项目中所有具有语义含义的识别工作，只允许使用 Codex 系列模型完成，具体包括：

- 页面图片中的文字、数学公式、题干、选项、解析和教材内容识别；
- 公式转写为 LaTeX/KaTeX；
- 题型、方法、答案、知识点和关系判断；
- 对配图是否存在及其页面内容的语义确认。

允许使用的识别模型仅为本工作流指定的 Codex 模型：`gpt-5.6-terra / high`（主识别与批量整理）和 `gpt-5.6-sol / high`（顺序准确性审计）。不得调用任何非 Codex 视觉模型、第三方大模型 API 或其他模型完成上述识别。

本地 PDF 工具只能用于文件读取、页码统计、文字层提取、页面渲染、哈希和机械格式检查；不得把本地 OCR 引擎（包括 Tesseract 等）的输出当作正式识别结果，也不得以其替代 Codex 对原始页面的视觉确认。任何 OCR 或自动文本提取结果如被保留，只能作为未采信的定位线索，不能直接写入已确认的题干、公式、答案或教材实体字段。

### 0.2 持续完成目标与临时错误恢复协议（新增硬约束）

每个章节会话必须以持续完成目标运行。401、429、503、`auth_unavailable`、连接中断、单次工具失败或单次子 Agent 失败均视为可恢复的临时事件，不是章节结束条件。

- `incomplete` 只能作为中间检查状态，不能作为没有真实阻塞时的正常结束理由；
- 发生临时错误时，先保存当前真实进度、待处理页码和下一游标，不得写入虚假的 `end_at`；
- 记录 `attempt`、错误类型、发生时间、`retry_after` 和 `not_before`，随后从上次成功页继续，不得重做已经确认的页面；
- 401、503 或 `auth_unavailable` 第一次出现后必须重新继续；429 必须遵守服务返回的 `retry_after` 后继续；
- 同一错误连续出现时，应先完成仍可执行的本地检查、页面准备和产物盘点，再恢复 Codex 识别；不得用页级摘要或占位文本替代失败的正式识别；
- 只有同一阻塞条件连续三个目标回合仍然存在、没有安全替代路径且无法继续任何有意义工作时，才可标记 `blocked`；必须列出三次尝试、已完成分母、剩余分母和恢复入口；
- 未达到最终验收或上述三回合真实阻塞条件前，不得发送表示章节任务结束的最终回复，必须重新继续完成。

以下行为一律视为未完成：

- 用模板批量复制题目评述；
- 只建立题目页，不定位已经存在的官方解析页；
- 用 AI 推导答案替代可获得的官方答案；
- 只检查 ID、数量、花括号或文件存在性，就宣称语义正确或 KaTeX 通过；
- 用少量概括句替代“教材定义、公式、重要结论、注意事项全部文字化”；
- 把 `needs_review` 当作已核准，或把尚未搜索到解析写成“没有官方解析”；
- 只完成小样、部分题号、Schema、脚本或目录结构后停止；
- 为满足一小时指标而缩减冻结范围、删除疑难项或虚构 `pass`。

六十分钟是效率目标，不是降低正确性的授权。若出现真实外部阻塞，必须保留已完成内容，列出精确阻塞项和剩余数量；不得把未完成章节写成完成。

## 1. 最终目标：三系统并行

### 1.1 教材核心知识系统

每章建立层级大纲，并完整文字化教材中明确出现的：

- 定义及其适用对象；
- 公式及其成立条件；
- 定理、性质、重要结论与推论；
- 注、注意、易错提醒、考点提示；
- 教材明确标出的“方法总结”“公式”“分析”中的可迁移结论；
- 与现有题目、选项或官方解析直接相关的关键教材内容。

教材普通叙述、证明和长推导不做逐句 OCR，只保存简短摘要和原页入口；这一限制不适用于题干和官方解析的 Codex 文字暂存。上述教材核心项必须逐项建立原子记录，不得把同页多个定义、性质或注意事项压缩成一条泛化摘要。

### 1.2 题目案例与方法系统

为每道1000题、教材例题和其他指定题目建立：

- 题面页；
- 官方解析页；
- 官方最终答案；
- 题目特定的 AI 评述；
- 题型、方法、教材知识点、二级结论；
- 关键技巧、关键判断和常见陷阱；
- 配图存在声明；
- 有明确证据支持的正向关系；已有关系的反向索引。

首轮必须先由 Codex 模型把冻结范围内的题干和官方解析页分别识别为文字，暂存后再组装题目记录。题干与解析的精确语义对应不作为首轮硬门槛；每道题的存储结构必须同时包含题干文字、解析文字、题面文件页数和解析文件页数。题型、方法、答案和 AI 评述可以在文字暂存完成后再生成。

### 1.3 个性化薄弱点系统

只根据用户主动提供的错题、疑问、错误解法、反复追问和上传材料生成。章节整理不得虚构用户掌握状态、薄弱点或复习进度。

### 1.4 三系统独立性与弱关系原则

教材核心知识系统、题目案例与方法系统、个性化薄弱点系统可以近乎独立地建立、追加和维护。首轮整理不要求三者之间形成完整、密集或逐条对应的关系网络。

关系记录遵循“有明确证据才建立、没有证据就留空”的原则：

- 题目、知识点、题型、方法和薄弱点之间的关系均可为空或后续追加；
- 只建立来源页、题面、解析或用户问题能够支持的关系，不为满足数量而推测关联；
- 反向索引只反映已经建立的关系，不要求覆盖所有题目或所有知识点；
- 验收只检查关系 ID 不断链、不重复、来源状态清楚，不检查关系数量或密度。

后续问答时，Agent 必须根据当前问题分别检索三个系统；如果多个系统对回答有帮助，可以组合教材证据、题目方法和用户薄弱点，但必须区分各自来源以及 AI 的综合推断。

## 2. 事实、推导与状态边界

### 2.1 允许的确认状态

- `source_page_confirmed`：本轮已经读取相应原始题面页或官方解析页，记录与该页一致。
- `agent_derived`：来源没有直接给出该结论，结果由 AI 推导；不得写成官方答案。
- `needs_review`：存在歧义、图像不可读、公式无法可靠辨认或来源相互冲突。
- `source_absent_confirmed`：已经检查完整冻结范围并有证据确认该资料确实不提供官方解析。仅仅“还没找到”不得使用此状态。

### 2.2 官方答案确认规则

只有同时满足以下条件，答案才可标为 `source_page_confirmed`：

1. 题面页已定位；
2. 官方解析或答案页已定位；
3. 已从官方页读取最终结果；
4. 最终结果覆盖题目要求的全部输出；
5. 页级映射已经写入并能打开对应原图。

如果官方资料存在，就禁止用 AI 自算结果代替官方答案。AI 可以独立计算用于交叉检查，但不能改变答案来源属性。

### 2.3 答案形态完整性

每题必须保存：

- `answer_kind`：选择、填空、计算、证明、参数、函数表达式、区间/分类或其他；
- `requested_outputs`：题目实际要求的全部量；
- `official_answer_text_or_katex`：官方最终答案；
- `answer_confirmation`：确认状态；
- `answer_source_mapping_id`：官方答案页映射。

具体规则：

- 选择题同时保存选项字母和选项所代表的结果；
- 参数题保存所有参数值，不得只写选项字母；
- 函数表达式题若题目要求定义域、值域或条件，必须一并保存；
- 间断点题保存位置、类型和个数中题目要求的全部内容；
- 证明题保存需证结论和官方解法使用的核心依据，不能只写“结论成立”；
- 多问问题不得漏掉任一问。

## 3. 来源范围必须先完整冻结

禁止逐题从整本 PDF 重新搜索解析。每章开始时必须批量建立并核对以下来源范围：

1. 基础题题面范围；
2. 基础题官方解析范围；
3. 强化题题面范围；
4. 强化题官方解析范围；
5. 教材正文范围；
6. 教材例题题面范围；
7. 教材例题官方解答范围，即使解答嵌在教材正文中；
8. 其他指定题集的题面与解析范围。

`source_scopes.jsonl` 每条至少包含文件、哈希、PDF 页范围、题号范围、角色、跨页风险、确认依据和置信度。

### 3.1 数学二学科级资料白名单

对于 `<subject>=math2` 的所有章节，本白名单按学科生效，不限定第一章或任何单一章节。后续章节只改变各文件中的章节页码和题号范围，不得增加资料文件。

允许使用的原始 PDF 仅有以下四份：

1. `27张宇基础30讲（高数）_新书签.pdf`：教材正文、定义、公式、结论、注释、方法总结和教材内官方例题解答；
2. `张宇30讲高数例题.pdf`：教材例题题面；
3. `27张宇1000题数二【试题册】.pdf`：张宇1000题基础题和强化题题面；
4. `27张宇1000题数二【解析册】.pdf`：上述基础题和强化题的对应官方解析。

明确禁止纳入：

- 李林880及其解析、题面或任何派生页图；
- 数二目录下除上述四份文件外的其他教材、题集、解析集和讲义；
- 用户未明确批准的新增资料；
- 旧知识库、旧章节产物或旧会话生成内容。

如果某章节在上述白名单资料中不存在对应内容，必须登记为 `source_absent_confirmed` 并保留范围检查证据；不得用李林或其他资料补齐，不得静默扩大白名单。

`source_scopes.jsonl`、`assets_manifest.jsonl`、覆盖分母和最终报告只能出现白名单中的四个 `source_id`。发现第五个资料文件时，必须阻塞该章节发布并报告来源越界。

冻结范围时必须明确区分：

- `textbook_example_question`；
- `textbook_example_official_solution`；
- `textbook_exposition`。

教材例题题面在单独例题集、解答在教材正文中的情况，首轮只需保留两者的原始文件和页码；如果能够确认解答页则写入，无法确认时保留解析文字暂存和待定位状态，不得把 AI 推导冒充官方解答。

## 4. 页级映射与题目注册表

首轮保持页级映射，不要求句级或 bbox 级定位。必须建立：

- 题目 → 题面页；
- 题目 → 官方解析页；
- 教材例题 → 例题题面页；
- 教材例题 → 教材官方解答页；
- 知识点、公式、结论、注意事项 → 教材页；
- 题目/例题 → 关键教材知识；
- 题型/方法/大纲节点 → 关联题目与例题。

先生成 `question_registry.jsonl`，再统一分类。每项至少包含稳定 ID、类别、题号、题面候选页、解析候选页、跨页风险、配图风险和初步结构标签。

解析页映射必须按“整章解析范围总览 → 题号起止位置 → 跨页题”一次性建立，不得为每题重新搜索整本解析册。首轮的对应关系只要求文件级和页级记录，不要求证明解析文字中的每一步与题干逐句对应。

### 4.1 Codex 文字识别暂存与题目记录字段

在建立题型、方法和弱关系之前，必须顺序完成两批独立的 Codex 识别：

1. 题册页面和教材例题题面 → `staging/recognized_question_pages.jsonl`；
2. 解析册页面以及教材正文中的官方解答页 → `staging/recognized_solution_pages.jsonl`。

每条暂存记录至少包含 `source_id`、PDF 页码、识别文本、LaTeX/KaTeX 公式、配图声明和识别状态。暂存文本必须保留，不得只保留模型摘要。

每道题的最终记录或题目包必须同时包含：

- `question_text`：Codex 从题面页识别的文字；
- `solution_text`：从解析页暂存文本中装入的官方解析文字；
- `question_source_file`、`question_source_pages`；
- `solution_source_file`、`solution_source_pages`；
- `mapping_status`：如 `page_range_assigned`、`page_link_only` 或真实待定位状态。

`solution_text` 的装入依据可以只是集中解析范围和页码分段；除非另有明确证据，不得声称已经完成句级或步骤级对应。

教材例题记录遵循同一原则：例题题面和教材正文中的官方解答分别识别、分别暂存，在 `examples.jsonl` 或例题包中同时保存可获得的题面文字、解答文字及各自来源页；无法确认解答页时保留文字和待定位状态。

## 5. 教材核心覆盖台账

为了在不做全文 OCR 的前提下证明教材核心没有被漏掉，每章新增必需文件：

`textbook_core_coverage.jsonl`

冻结范围内每一页至少有一条记录：

```json
{
  "source_id": "src.textbook",
  "pdf_page": 9,
  "page_checked": true,
  "core_item_counts": {
    "definitions": 1,
    "formulas": 0,
    "conclusions": 2,
    "notes": 1
  },
  "captured_entity_ids": [
    "kp.function.definition",
    "con.function.single_valued",
    "note.function.domain"
  ],
  "ordinary_text_summary_id": null,
  "status": "complete"
}
```

规则：

- 每页必须实际查看后才能写 `page_checked=true`；
- 页面没有核心项时可以计数为0，但仍需登记；
- 发现“定义、公式、性质、结论、推论、注、注意、方法总结、公式框、易错提醒”却没有对应实体 ID，则该页不得写 `complete`；
- 一页存在多个核心项时必须分别建实体；
- 大纲节点只是导航，不等于教材核心已文字化；
- `textbook_core_coverage.jsonl` 有缺页或未完成页时，章节不得发布。

## 6. 教材大纲与核心记录

`outline_nodes.jsonl` 必须形成真实层级：章节 → 节 → 知识模块 → 必要子模块。每个节点至少保存：

- 标题、层级、父节点；
- 起止页或明确入口页；
- 关联知识点、公式、结论、注意事项；
- `related_question_ids`；
- `related_example_ids`。

大纲节点不能只是平铺书签。若一个书签下包含定义、性质、比较、计算方法和注意事项，应拆成可导航子节点。

知识记录要求：

- 定义保存完整定义，不用一句解释性摘要代替；
- 公式保存 KaTeX、适用条件和页级来源；
- 结论说明是教材明确结论还是 AI 二级归纳；
- 注意事项保存触发条件和错误后果；
- AI 二级结论必须能指出来自哪些教材内容或哪些题目，不得伪装成教材原句。

## 7. 每题题目特定 AI 评述

每道题和教材例题的 AI 评述必须在实际读取题面与官方解析后生成，并回答：

1. 本题具体考什么；
2. 看到题面后第一步具体写什么或检查什么；
3. 官方解析使用了哪个关键公式、变形或判断；
4. 哪一步决定答案；
5. 本题最容易错在哪里；
6. 可迁移到哪类结构，以及迁移边界是什么。

至少包含一个本题特定的表达式、条件、参数、趋近方向、分段点或判断对象。以下内容单独出现不合格：

- “先识别结构”；
- “先找主导项”；
- “确认适用条件”；
- “可迁移到同类题”；
- “注意定义域”；
- 只写方法 ID、知识点 ID 或题型名称。

允许共享题型模板，但共享模板必须与 `item_specific_analysis` 分开保存；每题的 `item_specific_analysis` 不得为空。若两个不同题目的完整 `ai_review` 完全相同，最终验证必须报错，除非记录明确解释为什么二者内容完全同构。

## 8. 题型池与方法池

题型池至少保存识别特征、典型题干结构、第一步、常用知识、方法、混淆题型、关联1000题和关联教材例题。

方法池至少保存适用条件、识别信号、核心步骤、相关公式、二级结论、常见误用、关联题型、关联1000题和关联教材例题。

可选关联字段（允许为空，后续可追加）：

- `related_question_ids`；
- `related_example_ids`。

关系派生脚本只需把已经存在且来源明确的关系回填到相应反向索引；不得为了填满关系而推测关联。验收检查 ID 不断链、不重复和来源状态，不要求反向索引覆盖全部题目或例题。

## 9. 教材例题轻量记录

所有指定教材例题必须纳入。每题至少保存：

- 题面页及原图；
- 官方解答页及原图；
- 完整最终结果及必要条件；
- 题目特定 AI 评述；
- 关键方法、公式、教材知识点和二级结论；
- 题型池、方法池和考研题联系；
- 题面配图和解答配图分别声明。

`solution_page=null` 仅可用于 `source_absent_confirmed`，且必须给出检查过的完整范围和无解析证据。若教材正文已经包含讲解，则必须映射该页。

## 10. 配图声明

配图声明区分：

- `question_figure_presence`；
- `solution_figure_presence`；
- `figure_analysis_status`。

首轮不要求分析图形含义，但必须准确声明题面或解析中是否有图，并保留原页。不能因题面无图就把整个题目包写成 `figure_presence=false`。

## 11. 推荐目录结构

```text
knowledge_base_v04_light/
└─ <subject>/
   └─ <chapter>/
      ├─ chapter.json
      ├─ source_scopes.jsonl
      ├─ question_registry.jsonl
      ├─ page_mappings.jsonl
      ├─ staging/
      │  ├─ recognized_question_pages.jsonl
      │  └─ recognized_solution_pages.jsonl
      ├─ textbook_core_coverage.jsonl
      ├─ questions.jsonl
      ├─ examples.jsonl
      ├─ outline_nodes.jsonl
      ├─ knowledge_points.jsonl
      ├─ formulas.jsonl
      ├─ conclusions.jsonl
      ├─ problem_types.jsonl
      ├─ method_pool.jsonl
      ├─ relations.jsonl
      ├─ weak_points.jsonl
      ├─ assets_manifest.jsonl
      ├─ question_packages/
      ├─ example_packages/
      └─ reports/
         ├─ efficiency_report.md
         └─ acceptance_report.md
```

## 12. 后续章节工作流与阶段门

### 阶段 0：读取规则和可用参照

- 完整读取本文件；
- 读取最新已验收章节的字段格式和构建脚本；
- 后续章节只可参考 V0.4.1 规则及 V0.4.1 下通过验收的产物；
- 禁止读取或复制旧 `knowledge_base`、V0.3 第一章或旧第一章内容作为后续章节事实；
- 原始 PDF 和用户明确批准复用的来源页资产除外。

### 阶段 1：来源范围冻结门

- 冻结所有题面、官方解析、教材正文、例题题面和例题解答范围；
- 先批量确定每类题号覆盖和解析集中页范围；
- 只记录题面文件页和解析文件页的候选范围，不要求此阶段完成语义对应；
- 未完成所有类别的范围冻结，不得进入内容生成。

### 阶段 2：Codex 全量文字识别暂存门

- 由 `gpt-5.6-terra / high` 按页面批量读取全部 1000 题题面和教材例题题面，写入 `staging/recognized_question_pages.jsonl`；
- 再按页面批量读取全部 1000 题官方解析和教材正文中的官方例题解答，写入 `staging/recognized_solution_pages.jsonl`；
- 题干、解析、公式和配图声明必须保存为文字或 LaTeX 暂存记录，不得只保存摘要；
- 识别阶段不建立题目—解析的精细语义关系，识别不确定项标记状态并保留原页。

### 阶段 3：题目包组装与页级关联门

- 从两个暂存文件组装 `questions.jsonl` 和题目包；
- 每道题同时写入 `question_text` 与 `solution_text`；
- 同时保留题面文件/页数和解析文件/页数；
- 关系只要求文件级、页级和页码范围级，`mapping_status` 明确记录是否只是页码分段；
- 不要求逐句、逐步或知识点级对应；无法确认的高级关系留空。

### 阶段 4：教材核心覆盖门

- 逐页查看教材冻结范围；
- 填写 `textbook_core_coverage.jsonl`；
- 文字化所有核心项；
- 构建层级大纲；
- 覆盖台账有缺页或未捕获核心项时不得通过。

### 阶段 5：题目评述、题型池和方法池

- 依据题面和官方解析生成题目特定评述；
- 先合并题型与方法，再回填题目；
- 对有明确证据的关系生成正向记录，并按已有关系生成反向列表；
- 检查重复评述和泛化占位语句。

### 阶段 6：教材例题

- 批量建立例题题面—教材官方解答映射；
- 读取官方结果和关键方法；
- 分别声明题面图与解析图；
- 回填能够确认的题型、方法、知识点和反向索引，无法确认的关系留空并记录状态。

### 阶段 7：Sol 高风险准确性审计

批量内容阶段冻结后，顺序调用一次 `gpt-5.6-sol / high`，逐条核对：

- 所有题目最终答案与官方解析页；
- 所有教材例题最终结果与教材官方解答页；
- 所有题目/例题的解析文件与页码范围合法性；不审计逐句或逐步语义对应；
- 所有参数、正负号、指数、分式、根式、定义域、值域、间断点类型/个数和多问结果；
- 所有高风险教材公式的 KaTeX 语义与适用条件。

Sol 只输出逐条审计台账和问题清单，不重复生成整套知识库。主 Agent 必须处理台账中的每一条结果。

### 阶段 8：主 Agent 最终验收

主 Agent 不得仅相信批量内容 Agent 或 Sol 审计 Agent 的完成报告，必须直接读取产物并执行：

1. 冻结分母与实际记录数量对比；
2. 每题题面页和官方解析页存在性检查；
3. 每题答案形态完整性检查；
4. 所有官方答案来源状态检查；
5. 教材核心覆盖台账逐页无缺口检查；
6. 题目评述完全重复检测；
7. 题型池、方法和大纲中已有关系的反向索引检查；
8. ID 唯一性、关系引用不断链和关系来源状态检查；
9. 实际调用 KaTeX 编译全部公式，不得用括号计数代替；
10. 对所有疑似页码跨页、答案符号、指数、分式和参数结果进行视觉回看；
11. 检查 Sol 审计台账是否逐条覆盖全部题目和教材例题，并确认所有问题已修正或被列为未通过；
12. 输出 `acceptance_report.md`，分别报告结构、内容、来源、公式和语义验收。

## 13. 模型与 Agent 分工

本版本不把最高规格模型用于所有步骤。模型分配按“批量整理”和“高风险准确性”分离：

- 主协调 Agent：`gpt-5.6-terra / high`。
  - 负责读取规则、冻结分母、调度、合并文件、运行脚本、监督阶段门和最终发布判定。
  - 主协调工作以结构化调度和证据核对为主，不需要用 Sol 承担全部普通目录与索引操作。
- 默认批量内容 Agent：`gpt-5.6-terra / high`。
  - 负责一次性读取冻结范围，建立题目/例题记录、教材核心候选、题目特定评述和初步关系。
  - 不负责自行宣布完成，不设计新 Schema。
- 一次性准确性审计 Agent：`gpt-5.6-sol / high`。
  - 仅在批量内容阶段结束后顺序调用一次。
  - 必须覆盖所有已标记 `source_page_confirmed` 的题目答案、所有教材例题最终结果、题面/解析文件页码范围，以及所有高风险公式/参数/间断点结果；不要求逐句或逐步对应审计。
  - 输出逐条审计结果和错误清单，不重复生成整套知识库，不重写无问题记录。
  - 只对发现的冲突、错答、漏条件、错页和公式歧义提出修正；主 Agent 负责合并并复验。
- Luna 或更低规格模型不参与数学题面、官方答案、教材公式、结论和例题解答的识别。目录整理、ID检查、重复检测、关系完整性和 KaTeX 编译使用本地脚本完成。

同一章节最多有两个高强度阶段，而且必须顺序执行：

```text
Terra 主协调/批量整理  →  Sol 全量高风险答案审计  →  Terra 主协调合并与最终验收
```

不得并发启动两个内容 Agent；全局高强度内容槽位保持1，避免429。Sol 审计不是可选的第二次全章重做，而是一次有边界的逐条准确性检查。

遇到401、429、503、`auth_unavailable` 或连接中断时，必须按 0.2 节记录状态并从最后成功位置重新继续；不得因临时错误缩减分母、取消必要审计、写入占位内容或把未完成内容写成通过。

主 Agent 的职责不是转发提示词，而是监督阶段门、核对文件、读取 Sol 审计结果、纠正子 Agent 漏项并对最终完成声明负责。

## 14. 一小时目标的正确解释

目标仍是在来源范围已知、页面可正常读取的常规章节中，60分钟内完成合格首轮。推荐预算：

- 5分钟：规则、来源与范围冻结；
- 10分钟：全章题面/解析总览及答案配对；
- 15分钟：教材核心逐页覆盖台账与文字化；
- 15分钟：题目特定评述、题型池和方法池；
- 10分钟：教材例题题面—解答映射与评述；
- 5分钟：真实验证与验收报告。

如章节体量明显超过常规范围，先报告实测分母与预计时间，但仍需覆盖完整冻结范围。不得通过模板化评述、跳过教材解答、遗漏注意事项或伪造验证结果制造“一小时完成”。

## 15. 发布判据

只有同时满足以下条件，章节才可标记 `first_draft_accepted`：

1. 冻结范围内全部题目和例题有记录；
2. 全部题目与例题均有题面页；
3. 存在官方解析的项目全部有官方解析页；
4. 每道题的记录同时包含 Codex 识别的题干文字、解析文字及其来源文件页数；
5. 官方答案准确、完整，并覆盖题目要求的全部输出；
6. 教材核心覆盖台账逐页完整；
7. 定义、公式、重要结论、注意事项已逐项文字化；
8. 每题有题目特定评述；
9. 已建立的题型、方法和大纲关系能够反向找到对应题目或教材例题，未建立的关系允许为空；
10. 配图存在声明区分题面与解析；
11. 全部公式经真实 KaTeX 编译；
12. 没有未解决的高严重度答案、页码、来源或关系错误；
13. `acceptance_report.md` 与实际文件一致；
14. `needs_review` 不包含“尚未查找的官方答案或解析页”；
15. 主 Agent 已直接检查产物，而非只复述子 Agent 报告。

若任何一项不满足，章节状态必须是 `incomplete`、`blocked` 或 `draft_rejected`，不得使用“首轮完成”“发布完成”“全部通过”等表述。

## 16. 后续问答即时深化

当用户提出新题、错题或概念问题时：

1. 先检索教材大纲、核心知识、题型池、方法池和用户薄弱点；
2. 根据页级映射读取题面、官方解析或教材原页；
3. 只对当前问题需要的局部做详细转写和配图分析；
4. 新增内容写回相应题目、方法、题型、教材节点或薄弱点；
5. 保留来源、推导与用户信息三者的状态区别。

本机制用于保持首轮轻量，但不得反向成为首轮跳过官方答案、教材核心或题目评述的理由。

## 17. 第一章试验后的强制优化补充

第一章 V0.4.1 实际验收经验已整理至：

`E:\桌面\学习\考研\第一章V0.4.1验收经验与后续章节优化补充.md`

该文件是本版本的执行补充，后续章节必须同时阅读。关键硬规则如下：

1. 解析册集中页可以整页批量 Codex 转写；只要覆盖原页全部题号、步骤、结论和公式，不能因一页多题而标记为占位。历史 `rejected_placeholder`、`needs_review`、`pending` 必须在最终验收前按当前证据归一化，不能机械继承。
2. 脚本创建、子Agent自报完成和旧报告都不是完成证据。主Agent必须直接读取最新数据文件，核对更新时间、计数和状态。
3. LaTeX/KaTeX 写入必须避免 JavaScript 转义造成的控制字符；使用 `String.raw` 或正确 JSON 转义，并实际编译全部公式。
4. 非选择题必须有正式表达式、参数、区间、间断点或多问结果及来源页确认，不能因没有选项字母而留下 `needs_review`。
5. 关系只要求已有关系不断链、来源清楚，不追求密度；解析范围必须先批量冻结，避免逐题搜索整本PDF。
6. Terra high 负责批量内容阶段，Sol high 只做一次有边界的高风险审计，本地脚本负责机械验收；双Terra是否启动由第18节预检门决定，出现429或冲突必须回退单Terra串行。
7. 最终 `complete` 前必须同步证明题目字段、教材覆盖、解析状态、题型方法评述、关系、KaTeX、Sol台账、题目包状态和报告时间戳全部一致。

## 18. 后续章节效率优化与并行门

第一章试验表明，主要非必要耗时来自返工、旧状态和报告不同步，而不是正式识别本身。后续章节必须优先采用以下优化：

1. 主Agent在Terra识别期间并行准备页码范围、空记录、验收脚本、KaTeX预检和报告骨架；这些本地任务不得生成语义内容。
2. 正常页面按8–15页题面/教材批次、4–8页解析批次处理；每批只检查写入、计数和游标，错误后从游标续跑。
3. KaTeX按批预检并在最终全量编译前扫描JavaScript控制字符、命令前误插空格和未闭合环境。
4. 旧 `needs_review`、`rejected_placeholder`、`pending` 和过时报告必须在最终验收前依据当前原页证据统一归一化。

后续章节首选两个并行 `gpt-5.6-terra / high` 内容通道，随后一个 `gpt-5.6-sol / high` 高风险审计阶段。Terra A处理“1000题题面/解析/题目包”，Terra B处理“教材核心/教材例题/大纲”。双Terra启动前必须确认服务允许两个并发高强度调用、预检没有429、两个通道资料范围完全不重叠且各自使用隔离staging/输出目录；两通道合并并冻结后才可启动Sol。出现429、写入冲突、游标异常或来源不一致时必须立即切回单Terra串行模式，不得丢失已完成批次。

并行不得用于同一页、同一题目包或Sol审计；不得以并行或一小时目标缩减固定分母、官方解析、教材核心、题目评述、KaTeX或高风险审计。
