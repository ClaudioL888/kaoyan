interface:
  display_name: "数二知识库问答"
  short_description: "读取数二知识库答疑；课程内容归纳并经双重确认后保存"
  default_prompt: "Use $answer-math-kb to answer this Math II question, end course content with a concrete archive summary, and write only after I approve its accuracy and archiving."
policy:
  allow_implicit_invocation: true
