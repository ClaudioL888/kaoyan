# 数据结构第一章·绪论｜已接受完整复盘

> 记录时间：2026-08-19 20:05:01 +08:00
> 时间状态：既有内容回填（按用户指示，现有内容统一以 2026-08-19 为记录日期；不代表原题或资料发布日期）

> 归档说明：下列正文来自用户已验收的阶段一任务输出；仅增加本说明与批次分隔线，正文内容未改写。
> 来源任务：数据结构第一章选择题与应用题整理（01a01903-52a1-7870-9314-041acef6ca20）
> 用户确认时间：2026-08-19 17:29 +08:00

---
# 数据结构第一章·绪论｜阶段一完整第一版（待验收）

本版按更新后的两阶段合同重新生成；旧输出仅用于最后的差异审计，未作为本版正文或答案依据。当前处于阶段一，尚未保存、尚未进入知识库整理阶段。

---

# 批次 A：1.1 数据结构的基本概念（全部选择题）

## 1. 资料、版本与范围核对

- 教材：`2027数据结构_高清带书签版.pdf`，404 页。第一章印刷页 1—12，对应 PDF 阅读页 13—24；本批对应 1.1，印刷页 1—4、PDF 页 13—16。
- 题面来源：简约竖版选择题本，188 页；本批题面位于其 PDF 页 5—6，对应教材标记 `[P3]`。
- 本批范围：1.1 选择题 01—05，共 5 题、20 个选项；无跳号。
- 答案/解析：教材 1.1.4“答案与解析”，印刷页 4、PDF 页 16，可确认。
- 重复副本：选择题与应用题目录内的“原版书含答案”均与教材 SHA-256 完全相同，为教材重复副本，不构成独立答案证据。
- PDF 的 `startxref` 警告只影响部分书签读取；已按页码视觉核对题面与答案页，未见本范围内容缺失。教材：:codex-file-citation{path="E:\桌面\学习\考研\408\2027数据结构_高清带书签版.pdf" purpose="source"}；选择题本：:codex-file-citation{path="E:\桌面\学习\考研\408\27王道《复习指导数据结构》做题本\选择题\【简约竖版】27王道复习指导数据结构选择题.pdf" purpose="source"}；应用题本：:codex-file-citation{path="E:\桌面\学习\考研\408\27王道《复习指导数据结构》做题本\应用题\【简约竖版】27王道复习指导数据结构应用题.pdf" purpose="source"}。

## 2. 本范围知识体系总览

1.1 解决的是“如何完整描述数据组织”的问题：

- 数据元素是数据的基本单位；数据项组成数据元素。
- 逻辑结构描述元素间关系，不关心在内存中的具体安排。
- 存储结构是逻辑结构在计算机中的实现。
- 完整数据结构还必须规定基本操作。
- 线性/非线性是逻辑结构分类；顺序/链式是存储结构分类。

核心判断链：

`题干谈元素关系 → 逻辑结构`  
`题干谈数组、指针、连续地址 → 存储结构`  
`题干谈查找、插入、删除如何执行 → 基本操作或算法`

## 3. 核心知识、重点与难点系统讲解

### 知识点：数据结构的三要素

**教材依据：**印刷页 2—3、PDF 页 14—15。教材将数据结构概括为逻辑结构、存储结构及其基本操作。

**它解决什么问题：**既说明数据“彼此怎样关联”，又说明“怎样存进机器”，还说明“能对它做什么”。

**结构与操作：**

- 逻辑结构：如线性表、树、图。
- 存储结构：如顺序存储、链式存储。
- 基本操作：如插入、删除、查找、遍历。

**做题触发器：**看到“完整的数据结构” → 检查是否同时出现逻辑结构、存储结构、操作。

**边界：**只列“逻辑 + 存储”仍不完整；只列“数据元素及关系”也没有定义操作。

### 知识点：逻辑结构与存储结构分离

**教材依据：**印刷页 3—4、PDF 页 15—16。

**原理：**同一逻辑结构可用不同存储结构实现。例如线性表既可用顺序表实现，也可用链表实现。

**题型识别：**

- “线性、树、集合、图”通常谈逻辑关系。
- “顺序表、单链表、哈希表”通常谈具体存储或实现。
- “有序表”强调关键字之间的逻辑次序，不等同于某种特定实现。

**易错点：**把“顺序表”误当成“线性表”，或把“链表”误当成逻辑结构。

## 4. 教材二级结论、隐藏重点和典型案例

- 1.1.1 的数据项、数据元素、数据对象是由小到大的抽象层次；本批没有直接单选题考数据项，但它是判断“数据元素”选项的隐含基础。
- 树是非线性结构；字符串、栈、队列都具有线性关系。
- 逻辑结构不唯一决定存储结构；存储结构不是脱离逻辑结构独立存在。
- 教材 1.1.4 的例子表明，数据结构定义中的“基本操作”不可省略。

## 5. 重点难点易错点专项

| 判断点 | 正确情况 | 常见错误 | 真正条件 |
|---|---|---|---|
| 线性/非线性 | 树、图属于非线性 | 看到“表”就断定线性 | 看元素关系是否一对一串联 |
| 逻辑/存储 | 有序表可描述逻辑次序 | 把顺序表、单链表当逻辑结构 | 是否已指定数组/链结点实现 |
| 三要素 | 逻辑 + 存储 + 操作 | 漏掉基本操作 | 问“完整数据结构”必须三项齐全 |
| 独立性 | 逻辑结构独立于存储结构 | 误以为逻辑结构唯一决定存储 | 同一逻辑结构可多种实现 |

## 6. 选择题答案速查表

| 题号 | 最终答案 | 教材答案/解析 | 独立推导 | 冲突 |
|---|---|---|---|---|
| 1.1-01 | C | 教材印刷页 4：C | 三要素齐全 | 无 |
| 1.1-02 | A | 教材印刷页 4：A | 树为非线性 | 无 |
| 1.1-03 | C | 教材印刷页 4：C | 有序表描述逻辑次序 | 无 |
| 1.1-04 | A | 教材印刷页 4：A | 同一逻辑结构可多种存储 | 无 |
| 1.1-05 | C | 教材印刷页 4：C | 还须存元素间关系 | 无 |

## 7. 选择题逐题完整解析

### 【选择题】1.1-01｜答案：C

题干：一个完整的数据结构通常应包含以下哪些要素？

- A. 数据元素及其存储方式
- B. 数据的逻辑结构和物理结构
- C. 数据的逻辑结构、存储结构以及在其上定义的基本操作
- D. 数据对象和数据元素之间的关系

**考查核心：**数据结构的完整定义。

- A：错。数据元素与存储方式只覆盖对象和部分物理实现，未定义操作。
- B：错。逻辑结构与物理结构仍缺少“在其上定义的基本操作”。
- C：对。逻辑结构、存储结构、基本操作三项齐全；教材解析明确给出此答案。
- D：错。只谈对象与关系，既未给出存储方式，也未给出操作。

**独立推导：**若不知道插入、删除、查找等操作如何定义，就不能据此判断同一表示上的处理规则，因此不能称完整数据结构。

**第一反应：**题干出现“完整” → 找三要素最全的选项。

**易错诱因：**B 看起来接近定义，但少了操作。

**迁移：**判断抽象数据类型时也要同时看数据对象、关系、操作集合。

### 【选择题】1.1-02｜答案：A

题干：下列四种数据结构中，哪一个是非线性数据结构？

- A. 树
- B. 字符串
- C. 队列
- D. 栈

**考查核心：**线性与非线性逻辑结构。

- A：对。树中一个结点可有多个后继，属于层次型非线性关系。
- B：错。字符串是字符的有限序列，属于线性结构。
- C：错。队列按先进先出组织，元素关系是一对一的线性关系。
- D：错。栈按后进先出组织，仍是一对一的线性关系。

**独立推导：**线性结构中除首尾外每个元素通常有唯一直接前驱和直接后继；树不满足。

**第一反应：**看到“树、图”优先判断非线性。

**易错诱因：**队列有“排队”语义，但语义特殊不等于非线性。

**迁移：**二叉树、一般树、图均是非线性；数组和链表实现什么结构不改变其逻辑分类。

### 【选择题】1.1-03｜答案：C

题干：下列选项中，属于逻辑结构的是（ ）。

- A. 顺序表
- B. 哈希表
- C. 有序表
- D. 单链表

**考查核心：**逻辑结构与具体实现的区分。

- A：错。顺序表明确指定用连续存储单元实现线性表。
- B：错。哈希表通常描述散列组织与存取实现。
- C：对。有序表强调元素关键字之间的有序关系，不限定顺序或链式实现。
- D：错。单链表明确用结点与指针实现。

**独立推导：**只要去掉某种具体物理实现后概念仍成立，才更可能是逻辑层表述。

**第一反应：**选项中是否出现“顺序、链、哈希、指针”等实现词。

**易错诱因：**“顺序表”与“有序表”只差一个字，但前者是存储结构，后者是逻辑有序性。

**迁移：**“二叉树”是逻辑结构；“顺序存储二叉树”则加入了存储实现。

### 【选择题】1.1-04｜答案：A

题干：下列关于数据结构的说法中，正确的是（ ）。

- A. 数据的逻辑结构独立于其存储结构
- B. 数据的存储结构独立于其逻辑结构
- C. 数据的逻辑结构唯一决定其存储结构
- D. 数据结构仅由其逻辑结构和存储结构决定

**考查核心：**逻辑结构、存储结构与操作之间的关系。

- A：对。同一逻辑结构可以采用顺序、链式等不同存储实现。
- B：错。存储结构是逻辑结构在计算机中的映像，不是脱离逻辑结构自行定义。
- C：错。同一逻辑结构并不唯一对应某一种存储结构。
- D：错。遗漏了定义在其上的基本操作。

**独立推导：**线性表可顺序存储也可链式存储，直接否定 C；既然存储是某逻辑结构的实现，B 也不成立。

**第一反应：**遇到“唯一”“仅由”这类绝对词，优先检查是否漏掉多实现或基本操作。

**易错诱因：**把“逻辑结构独立于存储结构”错误地双向对称化。

**迁移：**抽象数据类型与具体数据结构的关系，也遵循“抽象先于实现”。

### 【选择题】1.1-05｜答案：C

题干：在存储数据时，通常不仅要存储各数据元素的值，还要存储（ ）。

- A. 数据的操作方法
- B. 数据元素的类型
- C. 数据元素之间的关系
- D. 数据的存取方法

**考查核心：**存储结构需要表现逻辑关系。

- A：错。操作方法属于算法/基本操作定义，不是每个元素必须附带存储的关系信息。
- B：错。类型可作为实现信息，但不是本题要强调的结构性内容。
- C：对。如链表中的指针、树结点的孩子指针、图的邻接信息都用于保存元素关系。
- D：错。存取方法是访问策略，不等同于元素间的关系表示。

**独立推导：**若只存值而没有下一个结点、邻接点或数组下标关系，无法恢复结构。

**第一反应：**“不仅存值” → 想“还要存关系”。

**易错诱因：**A、D 把“如何操作/访问”与“如何表示关系”混为一谈。

**迁移：**图的邻接矩阵、邻接表正是以不同方式保存同一类顶点关系。

## 8. 应用题答案/结论速查表

本批范围仅含 1.1 选择题；应用题按用户指定的安全批次全部置入批次 B 统一处理。因此本批无应用题答案项，不是遗漏。

## 9. 应用题逐题完整解析

不适用。本批不承载应用题；本章两道 1.1 应用题与一道 1.2 应用题均在批次 B 完整列题、解析全部小问。

## 10. 解析省略与资料冲突处理

- 题面来自选择题本；答案与解析来自教材相应章节，题本未另见独立答案页。
- 教材内置解析、题面和独立推导一致。
- “原版书含答案”文件哈希与教材相同，已排除为独立佐证。
- 本批没有无法辨认图表、跳号或版本冲突。

## 11. 题型识别与教材—题目迁移表

| 题型信号 | 第一反应 | 优先方法 | 教材联系 | 代表题 |
|---|---|---|---|---|
| “完整数据结构” | 补三要素 | 逻辑 + 存储 + 操作 | 1.1.1—1.1.2 | 01、04 |
| “非线性” | 看关系是否一对多/多对多 | 排除串行结构 | 1.1.2 | 02 |
| “属于逻辑结构” | 去掉实现词 | 区分抽象关系与实现 | 1.1.2 | 03 |
| “存储时还要什么” | 想到关系表示 | 指针/邻接/下标 | 1.1.2 | 05 |

## 12. 最终压缩总结

- 必须会写：数据结构 = 逻辑结构 + 存储结构 + 基本操作。
- 必须区分：线性/非线性是逻辑分类；顺序/链式是存储实现。
- 高频反例：线性表不是顺序表；顺序表不是有序表；操作集合不能省略。
- 自测：

1. 为什么“同一逻辑结构可有多个存储结构”？
2. 为什么“存储结构独立于逻辑结构”不成立？
3. 树为什么是非线性结构？
4. 指针在链表中保存的核心信息是什么？

## 13. 题目—知识点对应表与覆盖审计

| 应处理题号 | 已处理 | 选项覆盖 | 来源状态 | 未解决缺口 |
|---|---:|---:|---|---|
| 1.1-01 | 是 | A—D 全覆盖 | 题面、教材解析已核对 | 无 |
| 1.1-02 | 是 | A—D 全覆盖 | 题面、教材解析已核对 | 无 |
| 1.1-03 | 是 | A—D 全覆盖 | 题面、教材解析已核对 | 无 |
| 1.1-04 | 是 | A—D 全覆盖 | 题面、教材解析已核对 | 无 |
| 1.1-05 | 是 | A—D 全覆盖 | 题面、教材解析已核对 | 无 |

批次 A 覆盖率：5/5 题、20/20 选项。

AI 修正记录：无本批新发现的题面或答案错误；本版将旧输出中未显式分开的“逻辑结构/存储结构/操作”判断链拆开呈现。

## 14. 拟沉淀内容（尚未保存）

- 数据结构三要素。
- 逻辑结构与存储结构的区分规则。
- “顺序表/有序表/单链表”命名辨析。
- 本轮没有用户个人作答，不记录个人错误或薄弱点。
- 尚未保存，等待用户验收阶段一。

---

# 批次 B：1.2 全部选择题 + 本章全部应用题/小问

## 1. 资料、版本与范围核对

- 选择题范围：1.2 选择题 01—18，共 18 题、72 个选项；题面在选择题本 PDF 页 7—12。
- 应用题范围：本章 1.1 应用题 01—02、1.2 应用题 01；共 3 题、5 个可独立作答单元，其中 1.2-应用 01 含 3 个程序段小问。
- 教材位置：1.2 内容为印刷页 4—12、PDF 页 16—24；选择题解析主要在印刷页 8—11、PDF 页 20—23。
- 应用题题面：1.1 应用题在应用题本 PDF 页 5；1.2 应用题在 PDF 页 6。
- 答案角色：教材内置“答案与解析”提供选择题和应用题解析；题本用于题面、范围和顺序核对。
- 无跳号。题目数量、小问数均低于合同上限。

## 2. 本范围知识体系总览

1.2 的核心是算法定义与效率评价：

- 算法五特性：有穷性、确定性、可行性、输入、输出。
- 时间复杂度：用基本操作执行次数随输入规模的增长率衡量。
- 空间复杂度：通常指辅助空间随规模的增长率。
- 复杂度规则：
  - 串行语句取相加后的主导项；
  - 分支取最坏分支；
  - 固定次数或常量变量为 \(O(1)\)；
  - 循环变量乘常数变化，常见为对数；
  - 循环变量按单位递增直到其 \(k\) 次幂达到 \(n\)，常见为 \(O(n^{1/k})\)；
  - 嵌套循环必须逐层求和，不能机械相乘。

## 3. 核心知识、重点与难点系统讲解

### 知识点：算法五特性

**教材依据：**印刷页 4—5、PDF 页 16—17。教材列出有穷性、确定性、可行性、输入、输出。

**它解决什么问题：**区分“一个可以执行并终止的算法”与“只有想法或程序片段的描述”。

**做题触发器：**看到“算法应具有什么性质” → 直接核对五特性，不把可读性、可靠性、正确性混入定义。

**边界：**正确性、健壮性、可读性影响算法质量，但不是该教材此处定义的五特性。

### 知识点：时间复杂度与空间复杂度

**教材依据：**印刷页 5—8、PDF 页 17—20。

**原理：**

- 时间复杂度关注规模足够大时的主导增长项，忽略常数系数和低阶项。
- 空间复杂度关注算法运行所额外申请的辅助空间，不把输入本身占用错误计入辅助空间。
- \(O(n^2)\) 是渐近上界记号；具体题目若以教材标准答案表述“与 \(n^2\) 成正比”，应按题册语境选 C，但概念上不应把 \(O\) 严格等同于 \(\Theta\)。

### 知识点：循环、递归的复杂度判定

**教材案例：**印刷页 9—12、PDF 页 21—24，对倍增循环、递归和变上界嵌套循环给出分析。

**方法：**

- `i *= 2`：写 \(2^t\) 与 \(n\) 的不等式。
- `i++` 且 `i^k <= n`：写 \(t^k\le n\)。
- 外层倍增、内层长度为当前 `i`：求几何级数 \(1+2+\cdots\)，而不是 \(n\log n\)。
- 递归 `f(n/2)`：递归深度为 \(O(\log n)\)。
- 递归 `f(n-1)`：递归深度为 \(O(n)\)。

## 4. 教材二级结论、隐藏重点和典型案例

- 时间效率不等于真实 CPU 运行秒数；它以基本操作次数的渐近增长反映。
- 评价算法不应只看时空复杂度，也要考虑正确性、可读性、健壮性等。
- 同一逻辑结构采用不同存储方式，操作效率可以不同：顺序表中间插删常为 \(O(n)\)，链表在已定位时插删可为 \(O(1)\)。
- 变上界嵌套循环要展开成求和。  
  例如外层 `i*=2`、内层运行 `i` 次，总代价为 \(1+2+\cdots+2^k=O(n)\)，不是 \(O(n\log n)\)。

## 5. 重点难点易错点专项

| 场景 | 错误判断 | 正确判断条件 |
|---|---|---|
| \(O(1)\) 空间 | “不占任何空间” | 指辅助空间不随输入规模增长 |
| 复杂度比较 | 看常数或负低阶项 | 只看主导增长阶 |
| 有分支的代码 | 把两支相加 | 最坏时间取最大分支 |
| 双循环 | 一律外层 × 内层 | 内层上界是否依赖外层 |
| `i*=2` | 误作 \(O(n)\) | 解指数不等式 |
| `i^2<=n` | 误作 \(O(n)\) | 解幂次不等式 |
| `sum += ++i` | 误作 \(O(n)\) | 利用等差求和到达阈值 |
| 递归 | 只看函数式 | 看递归规模变化与单层成本 |

## 6. 选择题答案速查表

| 题号 | 答案 | 教材解析 | 独立推导 | 冲突 |
|---|---|---|---|---|
| 1.2-01 | B | 印刷页 8 | 五特性 | 无 |
| 1.2-02 | C | 印刷页 9 | 算法定义与评价 | 无 |
| 1.2-03 | C | 印刷页 9 | 题册语境采用教材结论 | 见第 10 部分术语提示 |
| 1.2-04 | B | 印刷页 9 | 辅助空间与 \(n\) 无关 | 无 |
| 1.2-05 | D | 印刷页 9 | 主导项比较 | 无 |
| 1.2-06 | D | 印刷页 9 | 倍增循环 | 无 |
| 1.2-07 | C | 印刷页 9 | 立方根边界 | 无 |
| 1.2-08 | D | 印刷页 9 | 二重求和 | 无 |
| 1.2-09 | A | 印刷页 9 | 最坏分支 | 无 |
| 1.2-10 | A | 印刷页 9 | \(\sum2i\) | 无 |
| 1.2-11 | C | 印刷页 9—10 | 递归深度 | 无 |
| 1.2-12 | A | 印刷页 10 | 倍增循环 | 无 |
| 1.2-13 | B | 印刷页 10 | 递归 \(n-1\) | 无 |
| 1.2-14 | C | 印刷页 10 | \(n\log n\) | 无 |
| 1.2-15 | B | 印刷页 10 | 三角数 | 无 |
| 1.2-16 | B | 印刷页 10 | 平方根边界 | 无 |
| 1.2-17 | B | 印刷页 10—11 | 几何级数 | 无 |
| 1.2-18 | B | 印刷页 11 | \(\sum_{i=1}^{\sqrt n}i\) | 无 |

## 7. 选择题逐题完整解析

### 【选择题】1.2-01｜答案：B

题干：一个算法应具有（ ）等重要特性。

- A. 可维护性、可读性和可行性
- B. 可行性、确定性和有穷性
- C. 确定性、有穷性和可靠性
- D. 可读性、正确性和可行性

- A：错。可维护性、可读性不是教材列出的算法五特性。
- B：对。可行性、确定性、有穷性都属于五特性。
- C：错。可靠性可评价质量，但不在本题五特性表述中。
- D：错。正确性、可读性重要，但不能替代输入、输出和有穷性等定义条件。

**第一反应：**算法特性题 → 回忆“有、确、可、入、出”。  
**易错点：**把软件工程质量属性误选为算法定义属性。  
**迁移：**写伪代码时应能指出输入、输出、终止和每一步可执行性。

### 【选择题】1.2-02｜答案：C

题干：下列关于算法的说法中，正确的是（ ）。

- A. 算法的时间效率取决于算法执行所花的 CPU 时间
- B. 在算法设计中不允许用牺牲空间效率的方式来换取更好的时间效率
- C. 算法必须具备有穷性、确定性等五个特性
- D. 通常用时间效率和空间效率来衡量算法的优劣

- A：错。时间复杂度关注基本操作次数的增长，不取决于某台机器的 CPU 秒数。
- B：错。空间换时间是常见设计权衡。
- C：对。符合教材五特性定义。
- D：错。时空效率重要，但评价还要考虑正确性、可读性、健壮性等。

**第一反应：**绝对词“不允许”“只用”通常要警惕。  
**易错点：**将“主要指标”夸大成“唯一指标”。  
**迁移：**哈希表以额外空间换取较快查找就是典型例子。

### 【选择题】1.2-03｜答案：C

题干：某算法的时间复杂度为 \(O(n^2)\)，则表示该算法的（ ）。

- A. 问题规模是 \(n^2\)
- B. 执行时间等于 \(n^2\)
- C. 执行时间与 \(n^2\) 成正比
- D. 问题规模与 \(n^2\) 成正比

- A：错。\(n\) 才是题设问题规模。
- B：错。大 O 忽略常数、低阶项，并非精确等式。
- C：对。按本题和教材解析的选择题表述，表示渐近量级为二次。
- D：错。把问题规模与算法代价混淆。

**术语边界：**严格数学上，\(T(n)=O(n^2)\) 只给出上界，并不必然推出 \(T(n)=\Theta(n^2)\)。本题教材解析采用 C 的教学性表述，因此最终按教材答案 C。

**迁移：**选择题按教材语境作答；书写严谨证明时应区分 \(O\)、\(\Omega\)、\(\Theta\)。

### 【选择题】1.2-04｜答案：B

题干：若某算法的空间复杂度为 \(O(1)\)，则表示该算法（ ）。

- A. 不需要任何辅助空间
- B. 所需辅助空间大小与问题规模 \(n\) 无关
- C. 不需要任何空间
- D. 所需空间大小与问题规模 \(n\) 无关

- A：错。可以使用有限个变量、指针或计数器。
- B：对。辅助空间为常数级。
- C：错。输入、程序本身仍需要空间。
- D：错。未限定“辅助”，若总空间包含输入，通常仍随 \(n\) 增长。

**第一反应：**见 \(O(1)\) 空间 → 补上“辅助”二字。  
**易错点：**把常量额外空间误写成零空间。

### 【选择题】1.2-05｜答案：D

题干：下列关于时间复杂度的函数中，时间复杂度最小的是（ ）。

- A. \(T_1(n)=n\log_2n+5000n\)
- B. \(T_2(n)=n^2-8000n\)
- C. \(T_3(n)=n\log_2n-6000n\)
- D. \(T_4(n)=20000\log_2n\)

- A：\(O(n\log n)\)，不是最小。
- B：\(O(n^2)\)，不是最小。
- C：\(O(n\log n)\)，不是最小；减去低阶线性项不改变主导阶。
- D：\(O(\log n)\)，对，增长最慢。

**第一反应：**先删常数系数和低阶项，再比较 \(\log n,n,n\log n,n^2\)。  
**易错点：**被 20000 这个大常数干扰。

### 【选择题】1.2-06｜答案：D

题干：下列算法的时间复杂度为（ ）。

```c
void fun(int n) {
    int i=1;
    while(i<=n)
        i=i*2;
}
```

- A. \(O(n)\)
- B. \(O(n^2)\)
- C. \(O(n\log_2n)\)
- D. \(O(\log_2n)\)

- A：错。`i` 不是每次加 1。
- B：错。无二重线性循环。
- C：错。没有每轮额外 \(n\) 级工作。
- D：对。第 \(t\) 次后 \(i=2^t\)，当 \(2^t>n\) 时结束。

**边界：**\(n<1\) 时循环不进入；通常默认 \(n\) 为正规模，不影响渐近结论。

### 【选择题】1.2-07｜答案：C

题干：下列算法的时间复杂度为（ ）。

```c
void fun(int n) {
    int i=0;
    while(i*i*i<=n)
        i++;
}
```

- A. \(O(n)\)
- B. \(O(n\log_2n)\)
- C. \(O(\sqrt[3]{n})\)
- D. \(O(\sqrt n)\)

- A：错。循环上界不是 \(n\)。
- B：错。无对数型乘倍过程。
- C：对。终止条件约为 \(i^3>n\)，故 \(i=\Theta(\sqrt[3]n)\)。
- D：错。平方根对应 \(i^2\le n\)，不是立方。

**方法：**变量按 1 增加但条件含 \(i^k\) → 迭代次数为 \(n^{1/k}\)。

### 【选择题】1.2-08｜答案：D

题干：冒泡排序程序中最后一行交换语句，在最坏情况下的频度为（ ）。

- A. \(O(n)\)
- B. \(O(n\log_2n)\)
- C. \(O(n^3)\)
- D. \(O(n^2)\)

- A：错。最坏逆序时，不是每轮只交换一次。
- B：错。冒泡排序没有对数层级。
- C：错。只有两层相关循环。
- D：对。逆序时每次比较都交换，次数为 \(\sum_{i=1}^{n-1}i=\Theta(n^2)\)。

**易错点：**题目问的是交换语句的最坏频度，不是某一趟的次数。

### 【选择题】1.2-09｜答案：A

题干：下列程序段的时间复杂度为（ ）。

```c
if(n>=0){
    for(int i=0;i<n;i++)
        for(int j=0;j<n;j++)
            printf("输入数据大于或等于零\n");
}else{
    for(int j=0;j<n;j++)
        printf("输入数据小于零\n");
}
```

- A. \(O(n^2)\)
- B. \(O(n)\)
- C. \(O(1)\)
- D. \(O(n\log_2n)\)

- A：对。按最坏情况，第一分支是二重循环。
- B：错。只看了第二分支。
- C：错。至少一个分支随 \(n\) 增长。
- D：错。没有对数型循环。

**第一反应：**`if/else` 的最坏时间取两条路径的最大量级，不相加。

### 【选择题】1.2-10｜答案：A

题干：下列算法中加下划线的语句 `m++` 的执行次数为（ ）。

```c
int m=0,i,j;
for(i=1;i<=n;i++)
    for(j=1;j<=2*i;j++)
        m++;
```

- A. \(n(n+1)\)
- B. \(n\)
- C. \(n+1\)
- D. \(n^2\)

- A：对。第 \(i\) 轮内层执行 \(2i\) 次，\(\sum_{i=1}^n2i=n(n+1)\)。
- B：错。遗漏内层循环。
- C：错。不是循环次数的精确和。
- D：错。其渐近阶同为 \(O(n^2)\)，但题目要求精确次数。

**易错点：**“执行次数”与“时间复杂度”不是同一个提问。

### 【选择题】1.2-11｜答案：C

题干：下列函数代码的时间复杂度是（ ）。

```c
int Func(int n) {
    if(n==1) return 1;
    else return 2*Func(n/2)+n;
}
```

- A. \(O(n)\)
- B. \(O(n\log_2n)\)
- C. \(O(\log_2n)\)
- D. \(O(n^2)\)

- A：错。本题按函数调用时间分析，递归规模每次减半。
- B：错。每层未进行长度为 \(n\) 的循环。
- C：对。递归调用深度约为 \(\log_2n\)，每层除调用外为常量基本工作。
- D：错。无平方级展开。

**边界：**表达式 `2*Func(n/2)+n` 中乘 2 和加 \(n\) 是常数次算术，不是两次递归调用；若写成 `Func(n/2)+Func(n/2)` 才是另一道递归树问题。

### 【选择题】1.2-12｜答案：A

题干：设 \(n\) 是描述问题规模的非负整数，下列程序段的时间复杂度是（ ）。

```c
x=2;
while(x<n/2)
    x=2*x;
```

- A. \(O(\log_2n)\)
- B. \(O(n)\)
- C. \(O(n\log_2n)\)
- D. \(O(n^2)\)

- A：对。每次翻倍，约需 \(\log_2(n/2)-1\) 次。
- B：错。不是线性递增。
- C：错。没有内层线性工作。
- D：错。无二重循环。

### 【选择题】1.2-13｜答案：B

题干：求整数 \(n(n\ge0)\) 阶乘的算法如下，其时间复杂度是（ ）。

```c
int fact(int n) {
    if(n<=1) return 1;
    return n*fact(n-1);
}
```

- A. \(O(\log_2n)\)
- B. \(O(n)\)
- C. \(O(n\log_2n)\)
- D. \(O(n^2)\)

- A：错。递归规模不是折半。
- B：对。调用链为 \(n,n-1,\ldots,1\)，约 \(n\) 层。
- C：错。无对数深度。
- D：错。每层为常数工作。

**空间补充：**递归栈深度为 \(O(n)\)，但题目只问时间。

### 【选择题】1.2-14｜答案：C

题干：下列程序段的时间复杂度是（ ）。

```c
count=0;
for(k=1;k<=n;k*=2)
    for(j=1;j<=n;j++)
        count++;
```

- A. \(O(\log_2n)\)
- B. \(O(n)\)
- C. \(O(n\log_2n)\)
- D. \(O(n^2)\)

- A：错。只计算了外层次数。
- B：错。只计算了每轮内层成本。
- C：对。外层 \(\Theta(\log n)\) 次，每轮内层 \(n\) 次。
- D：错。外层不是 \(n\) 次。

### 【选择题】1.2-15｜答案：B

题干：下列函数的时间复杂度是（ ）。

```c
int func(int n){
    int i=0, sum=0;
    while(sum<n)
        sum += ++i;
    return i;
}
```

- A. \(O(\log_2n)\)
- B. \(O(n^{1/2})\)
- C. \(O(n)\)
- D. \(O(n\log_2n)\)

- A：错。变量并非倍增。
- B：对。循环后 \(sum=1+2+\cdots+i=i(i+1)/2\)，令其达到 \(n\)，有 \(i=\Theta(\sqrt n)\)。
- C：错。虽然 `i` 逐一增加，但它不需要增长到 \(n\)。
- D：错。无嵌套或对数层。

### 【选择题】1.2-16｜答案：B

题干：设 \(n\) 是描述问题规模的非负整数，下列程序段的时间复杂度是（ ）。

```c
x=0;
while(n>=(x+1)*(x+1))
    x=x+1;
```

- A. \(O(\log_2n)\)
- B. \(O(n^{1/2})\)
- C. \(O(n)\)
- D. \(O(n^2)\)

- A：错。没有倍增。
- B：对。第 \(k\) 次循环前约有 \(x=k-1\)，结束条件对应 \(k^2>n\)。
- C：错。循环次数仅到平方根级。
- D：错。无双层迭代。

### 【选择题】1.2-17｜答案：B

题干：下列程序段的时间复杂度是（ ）。

```c
int sum=0;
for(int i=1;i<n;i*=2)
    for(int j=0;j<i;j++)
        sum++;
```

- A. \(O(\log_2n)\)
- B. \(O(n)\)
- C. \(O(n\log_2n)\)
- D. \(O(n^2)\)

- A：错。忽略了内层随 \(i\) 增长的成本。
- B：对。总次数是 \(1+2+4+\cdots+2^t<2n\)。
- C：错。不能将“外层 \(\log n\)”和“最大内层 \(n\)”机械相乘。
- D：错。总和是几何级数，不是平方级。

**关键状态：**外层 `i` 依次为 \(1,2,4,\ldots\)，第 \(r\) 层内层恰执行 \(2^r\) 次。

### 【选择题】1.2-18｜答案：B

题干：下列程序段的时间复杂度是（ ）。

```c
int count=0,i,j;
for(i=1;i*i<=n;i++)
    for(j=1;j<=i;j++)
        count++;
```

- A. \(O(\log_2n)\)
- B. \(O(n)\)
- C. \(O(n\log_2n)\)
- D. \(O(n^2)\)

- A：错。外层到 \(\sqrt n\)，不是对数。
- B：对。总次数为 \(\sum_{i=1}^{\lfloor\sqrt n\rfloor}i=\Theta(n)\)。
- C：错。无对数层。
- D：错。上界不应按最大内层 \(\sqrt n\) 与外层 \(\sqrt n\) 机械近似后又误写成平方。

**迁移：**外层到 \(\sqrt n\)、内层到 \(i\) 时，优先写三角和，而非只看两层循环。

## 8. 应用题答案/结论速查表

| 题号/小问 | 最终结论 |
|---|---|
| 1.1-应用 01 | 不一定；不同数据结构可拥有相同逻辑和物理结构，但操作或效率不同 |
| 1.1-应用 02 | 例：线性表的顺序存储与链式存储；随机访问/插删效率不同 |
| 1.2-应用 01（1） | \(T(n)=\Theta(n)\)，辅助空间 \(O(1)\) |
| 1.2-应用 01（2） | \(T(n)=\Theta(\sqrt n)\)，辅助空间 \(O(1)\) |
| 1.2-应用 01（3） | \(T(n,m)=\Theta(nm)\)，辅助空间 \(O(1)\)；若计数组 \(a\)，总存储 \(\Theta(nm)\) |

## 9. 应用题逐题完整解析

### 【应用题】1.1-应用 01

题干：对于两种不同的数据结构，逻辑结构或物理结构一定不相同吗？

**题目要求与输入状态：**判断“不同数据结构”是否必然要求逻辑结构或物理结构不同。

**解题思路：**数据结构的区分不只来自逻辑与存储表示，定义在结构上的操作、约束与效率也可造成区别。

**关键过程：**

- 二叉树与二叉排序树都可采用二叉树的逻辑表示和链式存储表示。
- 二叉排序树额外具有左子树关键字小于根、右子树关键字大于根的有序约束。
- 在查找操作上，一般二叉树可能需要遍历，二叉排序树可利用有序性进行方向选择。

**结果：**不一定。两种不同数据结构的逻辑结构和物理结构可以相同，但其数据约束、操作语义或操作效率可不同。

**复杂度与边界：**

- 一般二叉树查找最坏 \(O(n)\)。
- 二叉排序树查找平均可为 \(O(\log n)\)，但退化为链时最坏仍为 \(O(n)\)。
- 不要把“二叉排序树”误称为“必然 \(O(\log n)\)”；是否平衡影响最坏复杂度。

**迁移：**比较两个结构时，不要只比较存储图；还要比较不变量、允许操作与性能目标。

### 【应用题】1.1-应用 02

题干：举例说明对相同的逻辑结构，同一种运算在不同存储方式下实现时，其运算效率不同。

**题目要求与输入状态：**给出同一逻辑结构、同一种操作、两种存储实现及效率对比。

**解题思路：**选择线性表，分别用顺序存储和链式存储实现。

**关键状态/过程：**

- 顺序表：元素连续存放。中间插入一个元素，插入位置后的元素需后移。
- 链表：节点以指针相连。若已经定位到插入位置的前驱，只需修改两个链接关系。

**AI 改写伪代码：**

```text
顺序表在第 i 位插入 x：
for j = n downto i:
    A[j + 1] = A[j]
A[i] = x
```

```text
单链表在 p 后插入新结点 s：
s.next = p.next
p.next = s
```

**结果与复杂度：**

| 存储方式 | 按序号访问 | 已定位处插入/删除 |
|---|---:|---:|
| 顺序表 | \(O(1)\) | \(O(n)\) |
| 链表 | \(O(n)\) | \(O(1)\) |

**核查与边界：**

- 链表“插入 \(O(1)\)”以已知 `p` 为前提；若要先找第 \(i\) 个位置，总时间为 \(O(n)\)。
- 顺序表尾部插入在容量足够时可为 \(O(1)\)，中间位置才一般为 \(O(n)\)。
- 顺序表扩容时可能有一次 \(O(n)\) 复制成本。

**迁移：**看到“随机访问多”偏向顺序存储；看到“已定位后的频繁插删”偏向链式存储。

### 【应用题】1.2-应用 01

题干：分析下列各程序段，求出算法的时间复杂度。

#### （1）

```c
i=1; k=0;
while(i<n-1) {
    k=k+10*i;
    i++;
}
```

**输入状态：**规模为 \(n\)，`i` 从 1 每轮加 1。

**关键状态：**

| 循环轮次 | `i` 进入循环时 | 主体是否执行 |
|---|---:|---|
| 1 | 1 | 是（若 \(n>2\)） |
| 2 | 2 | 是 |
| … | … | … |
| 最后 | \(n-2\) | 是 |
| 结束 | \(n-1\) | 否 |

**独立推导：**主体共执行 \(n-2\) 次，因此 \(T(n)=\Theta(n)\)。

**教材对照：**教材解析同样给出 \(n-2\) 次、\(O(n)\)。

**空间：**`i,k` 为常数个变量，辅助空间 \(O(1)\)。

**边界：**当 \(n\le2\) 时循环 0 次，不影响渐近结论。

#### （2）

```c
y=0;
while((y+1)*(y+1)<=n)
    y=y+1;
```

**输入状态：**`y` 从 0 按 1 增长，停止条件由平方与 \(n\) 比较。

**关键状态：**

| 执行次数 \(t\) | 循环后 `y` | 条件关系 |
|---:|---:|---|
| 0 | 0 | \(1^2>n\) 时直接结束 |
| 1 | 1 | \(1^2\le n\) |
| \(t\) | \(t\) | \(t^2\le n\) |
| 结束 | \(\lfloor\sqrt n\rfloor\) | \((y+1)^2>n\) |

**独立推导：**循环 \(t\) 次须满足 \(t^2\le n\)，故 \(t=\Theta(\sqrt n)\)。

**教材对照：**教材给出 \(T(n)=O(n^{1/2})\)。

**空间：**辅助空间 \(O(1)\)。

**迁移：**若把条件改为 \((y+1)^3\le n\)，则为 \(O(\sqrt[3]n)\)。

#### （3）

```c
for(i=0; i<n; i++)
    for(j=0; j<m; j++)
        a[i][j]=0;
```

**输入状态：**待置零的二维数组 `a`，共有 \(n\) 行、\(m\) 列。

**关键状态：**

- 外层每个 `i` 固定时，内层完整访问 \(j=0,\ldots,m-1\)。
- 每个数组位置 `a[i][j]` 恰赋值一次。
- 总赋值次数为 \(n\times m\)。

**独立推导：**  
\[
T(n,m)=\sum_{i=0}^{n-1}\sum_{j=0}^{m-1}1=nm=\Theta(nm)
\]

**教材对照：**教材答案给出 \(O(mn)\)，与 \(O(nm)\) 等价。

**空间：**

- 不计已存在或作为输出的数组 `a`：辅助空间 \(O(1)\)。
- 若题目问整个数组存储量：\(\Theta(nm)\)。

**边界：**\(n=0\) 或 \(m=0\) 时不执行；不改变复杂度表达式。

## 10. 解析省略与资料冲突处理

| 项目 | 教材/题册情况 | 本版处理 |
|---|---|---|
| 选择题答案 | 教材有逐题答案与解释 | 采用教材答案，并独立复算 |
| 应用题 1.1-01 | 教材给出简要反例说明 | 补充了“有序约束、平衡与退化”边界 |
| 应用题 1.1-02 | 教材给出顺序表/链表对比 | 补充“已定位”前提与尾插例外 |
| 1.2-03 的 \(O(n^2)\) | 教材答案为 C，并使用“成正比”表述 | 选择题答案仍取 C；另标出 \(O\) 与 \(\Theta\) 的严格术语边界 |
| 文件副本 | “原版书含答案”与教材哈希一致 | 仅当教材副本，不独立计证 |
| 题面/答案冲突 | 未发现 | 无需裁决 |

## 11. 题型识别与教材—题目迁移表

| 题目信号 | 第一反应 | 优先方法 | 过程表示 | 复杂度/边界 | 代表题 |
|---|---|---|---|---|---|
| 算法特性 | 回忆五特性 | 排除软件质量属性 | 输入—输出—终止 | 有穷、确定、可行 | 01、02 |
| \(O(1)\) 空间 | 加上“辅助” | 区分输入与额外变量 | 变量个数 | 与 \(n\) 无关 | 04 |
| 多个复杂度函数 | 取主导项 | 去系数与低阶项 | 增长阶比较 | 渐近而非小 \(n\) 数值 | 05 |
| `i*=2` | 写 \(2^t\) | 解指数不等式 | 变量倍增表 | \(O(\log n)\) | 06、12、14 |
| `i^k<=n` | 写 \(t^k\) | 解幂次不等式 | 变量增长表 | \(O(n^{1/k})\) | 07、16 |
| 分支代码 | 取最大分支 | 最坏情况分析 | 分支成本表 | 不相加 | 09 |
| 变上界双循环 | 写求和 | 等差/几何级数 | 每轮内层次数 | 防机械相乘 | 10、17、18 |
| 递归 | 看规模变化 | 数递归深度 | 调用链 | \(n/2\) 与 \(n-1\) 区分 | 11、13 |
| 线性表存储比较 | 先问操作目标 | 顺序/链式取舍 | 移动元素/改指针 | 已定位前提 | 1.1 应用 02 |

## 12. 最终压缩总结

### 必须掌握

- 算法五特性：有穷性、确定性、可行性、输入、输出。
- \(O(1)\) 空间指辅助空间不随规模增长，不是“零空间”。
- 倍增循环：\(\log n\)。
- 逐一增加直到平方/立方阈值：\(\sqrt n/\sqrt[3]n\)。
- 外层倍增、内层为当前外层值：几何级数，常为 \(O(n)\)。
- 已定位链表插删 \(O(1)\)；顺序表按下标访问 \(O(1)\)。

### 最值得复习的教材案例

- 线性表顺序/链式实现的效率比较。
- 变量参与循环条件时，先列不等式再解次数。
- 递归 `n/2` 与 `n-1` 的深度对比。

### 闭卷自测

1. 写出算法五特性，并说明可读性为何不在其中。
2. 为什么 \(O(1)\) 空间不等于不需要空间？
3. `i*=2`、`i++且i*i<=n`、`i++且i*i*i<=n` 分别是什么复杂度？
4. 为什么题 17 不是 \(O(n\log n)\)？
5. 链表插入为什么必须加“已定位”前提？
6. \(T(n)=O(n^2)\) 与 \(T(n)=\Theta(n^2)\) 有何区别？

## 13. 题目—知识点对应表与覆盖审计

| 范围 | 应处理 | 已处理 | 覆盖细则 | 来源状态 | 缺口 |
|---|---:|---:|---|---|---|
| 1.2 选择题 | 18 题 | 18 题 | 72/72 选项 A—D 已判断 | 题面与教材解析已核 | 无 |
| 1.1 应用题 | 2 题 | 2 题 | 2/2 结论已完整展开 | 题面、教材解析已核 | 无 |
| 1.2 应用题 | 1 题、3 小问 | 1 题、3/3 小问 | 代码、推导、复杂度、边界均已覆盖 | 题面、教材解析已核 | 无 |

批次 B 覆盖率：21/21 编号题、5/5 应用题作答单元、72/72 选择项。

AI 修正记录：

1. 不再把教材重复 PDF 当作独立答案文件。
2. 对 \(O(n^2)\) 的题目保留教材答案 C，同时补充 \(O\) 与 \(\Theta\) 的严格区别。
3. 对链表插入的 \(O(1)\) 显式加入“已定位到前驱”的前提。
4. 对二叉排序树的 \(O(\log n)\) 显式限定为平均或平衡情形，避免误称最坏情形。
5. 对二维数组题明确区分辅助空间 \(O(1)\) 与数组总存储 \(\Theta(nm)\)。

## 14. 拟沉淀内容（尚未保存）

- 算法五特性与复杂度判定模板。
- 指数、幂次、三角和、几何和四类循环分析方法。
- 分支最坏时间与变上界嵌套循环的判断规则。
- 顺序表/链表的操作效率条件。
- 本轮无用户真实作答、犹豫或自述，因此无个人错误条目。
- 尚未保存，等待用户验收阶段一。

---

# 全章覆盖审计、跨批迁移与旧版比较

## 全章范围级覆盖审计

| 项目 | 应处理 | 已处理 | 状态 |
|---|---:|---:|---|
| 教材真实目录 | 1.1、1.2 | 2/2 | 完成 |
| 选择题 | 23 题 | 23/23 | 完成 |
| 选择题选项 | 92 项 | 92/92 | 完成 |
| 应用题编号题 | 3 题 | 3/3 | 完成 |
| 应用题独立作答单元 | 5 个 | 5/5 | 完成 |
| 题面来源 | 选择题本、应用题本 | 均已视觉核对 | 完成 |
| 答案来源 | 教材内置答案与解析 | 23 题 + 5 单元已对照 | 完成 |
| 未解决证据缺口 | 0 项 | 0 项 | 无 |

## 跨批方法迁移

| 从哪里看到 | 应迁移出的第一反应 |
|---|---|
| 数据结构定义题 | 先拆“关系、存储、操作”三个层次 |
| 存储方式比较题 | 先问核心操作是什么，再谈效率 |
| 复杂度函数比较 | 去常数、去低阶，只比主导增长 |
| 条件含变量幂次 | 把循环次数设为 \(t\)，解 \(t^k\le n\) |
| 递归题 | 明确一次还是多次递归调用，再看规模缩减方式 |
| 双循环题 | 先写内层次数序列，再求和，不机械相乘 |

## 与旧版相比的新增/修正候选

- 新增：按合同完整拆为批次 A、批次 B，并且每批均保留 14 个固定部分。
- 新增：每题给出来源对照、独立推导、第一反应、易错诱因与迁移。
- 新增：应用题补齐输入状态、关键状态、伪代码、边界和条件变化。
- 修正：将“教材答案”明确为教材内置答案与解析，不将哈希重复副本作为独立证据。
- 修正：对 1.2-03 的大 O 表述作严格术语补充，保持教材题目答案不变。
- 修正：补出全章 92 个选择项、5 个应用题作答单元的覆盖审计。
- 保留：23 道选择题答案与 3 道应用题的最终结论均与旧版一致；本版的改动是证据边界、完整度和条件限定的增强。

阶段一完整第一版至此结束，当前状态为：**待用户验收；未保存、未写入任何知识库。**

<oai-mem-citation>
<citation_entries>
MEMORY.md:209-211|note=[used source-faithful complete option-level review conventions]
MEMORY.md:216-220|note=[used PDF source, duplicate-hash, and rendering verification guidance]
</citation_entries>
<rollout_ids>
01a00d96-f78e-74d0-b590-9505463cdb6e
</rollout_ids>
</oai-mem-citation>
