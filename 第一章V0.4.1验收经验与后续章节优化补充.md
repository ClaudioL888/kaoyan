# 第一章 V0.4.1 验收经验与后续章节优化补充

用途：本文件记录第一章“识别优先轻量流程”实际运行后验证有效的提速、纠错和验收经验。它是 V0.4.1 的执行补充，不改变四份资料白名单、存储结构、弱关系原则或完成判据。

## 一、已经验证有效的工作顺序

1. 先冻结题面、解析、教材正文和教材例题的集中页范围，并建立固定分母。
2. 先按页批量识别全部题面，再按页批量识别全部官方解析；识别文本保存在 staging 中。
3. 再依据题号、页码范围组装每道题的 `question_text`、`solution_text`、题面页和解析页。
4. 教材核心按页检查，定义、公式、结论和注意事项分别写入原子记录。
5. 全部文字暂存完成后，再统一生成题型、方法和本题特定评述。
6. 最后只做一次有边界的 Sol high 高风险审计和一次主Agent直接文件验收。

这种顺序避免了每道题重复搜索整本解析册，也避免在题目尚未完整识别时过早建立关系。

## 二、必须吸收的正确性经验

### 2.1 页级批量转写可以合格，但必须真实完整

解析页通常集中包含多道题。只要 Codex 转写覆盖该原页的全部题号、步骤、结论和公式，页级记录就是正式识别结果，不得因为“一页不止一道题”而标记为占位或拒绝。之后在题目包中按页码分段装入每题 `solution_text`。

历史 `rejected_placeholder`、`needs_review` 或 `pending` 状态不能永久继承。每次最终验收前必须依据当前原页证据重新归一化状态；确有缺失才保留，已补齐则清除并记录清除依据。

### 2.2 “脚本已生成”不等于“产物已完成”

子Agent可能只创建写入脚本、验收脚本或报告脚本而未执行。主Agent必须检查实际数据文件、更新时间和报告内容，确认脚本已经运行。旧的 `acceptance_report.md`、`katex_validation.json` 或 `progress_cursor.json` 不能代表当前状态，必须核对其时间和计数是否对应最新产物。

### 2.3 公式必须从源头避免转义损坏

JavaScript 普通字符串中的 `\f` 会变成控制字符，曾导致 `\\frac` 在 KaTeX 中失败。后续写入公式时：

- 优先使用 `String.raw`、JSON 正确转义或独立 JSON 数据；
- 禁止在普通 JS 字符串中直接写未转义的 LaTeX 命令；
- 编译前扫描控制字符、命令前误插空格和未闭合环境；
- 必须用本地 KaTeX 运行时编译全部正式公式，并把失败数量写入报告。

非空、花括号平衡或单条冒烟公式通过，均不能替代全量编译。

### 2.4 非选择题的答案状态不能留空

最终机械验收应特别扫描填空、函数表达式、参数、区间、间断点和多问题。即使没有选项字母，也必须从官方解析填写正式表达式或全部参数，并把 `answer_confirmation` 改为来源页确认；不能因“没有选项”长期保留 `needs_review`。

### 2.5 关系稀疏不是错误，断链才是错误

关系只在原页或解析有明确证据时建立。第一章实际只需保留少量可靠关系；验收检查已写关系的 ID、来源和反向索引不断链，不把关系数量或密度作为质量指标。

## 三、必须吸收的效率经验

### 3.1 一次确定解析集中范围

先确定基础题解析页范围、强化题解析页范围和跨页题，再按题号划分；禁止每道题重新从整本解析册搜索。该做法是本章节省时间最明显的环节。

### 3.2 检查点要轻量且可续跑

每个页批次写入后只确认三件事：文件写入成功、计数增加、游标推进。401/429/503 或命令失败时从游标继续，不重做已确认页；不要把临时恢复升级成全章二次识别。

### 3.3 模型分工固定

- `gpt-5.6-terra / high`：一次批量视觉识别、题目组装、教材核心和题目评述；
- `gpt-5.6-sol / high`：批量完成后只做一次高风险答案/公式/页码审计；
- 本地脚本：分母、ID、状态、关系、重复、KaTeX和报告机械检查。

不得让 Sol 重复生成整套文本；双Terra只有在本文件第6.1和6.3节预检条件全部满足时才可启动。这样既降低429风险，也避免不同Agent对同一页产生冲突版本。

### 3.4 六十分钟是目标，不是截断点

第一章实际耗时超过一小时，主要原因是教材75页核心逐页确认、教材32道解答定位和错误状态清理。后续应通过批量页范围和脚本化验收提速，但不得为了达到60分钟删除分母、跳过教材解答或取消必要审计。必须报告真实墙钟时间。

## 四、后续章节的统一最终验收清单

主Agent在写 `complete` 或 `first_draft_accepted` 前，必须直接读取并同时证明：

- 所有题目 `question_text`、`solution_text`、题面页、解析页齐全；
- 所有教材例题/习题有题干和官方解答；
- 解析页批量转写无未解释的历史拒绝状态；
- 每题题型、方法和本题特定评述齐全且不是统一模板；
- 教材核心覆盖台账逐页完整；
- 已有关系无断链、无重复、来源明确；
- 全量 KaTeX 编译失败数为0；
- Sol 高风险台账覆盖固定分母且无 `unresolved`；
- 题目包状态不再是 `pending`；
- `acceptance_report.md`、`efficiency_report.md`、游标和实际文件的时间/计数一致。

任何一项只在脚本或子Agent消息中出现、而未在当前文件中得到证据，都视为未完成。

## 五、来源与版本边界

后续章节可以借鉴第一章通过 V0.4.1 验收后的字段格式、目录结构、脚本模式和本文件经验；不得读取旧第一章产物中的答案、评述、教材实体或关系作为事实。数学二仍只允许 V0.4.1 规定的四份PDF，李林和其他资料不得进入任何范围、哈希、页面渲染或报告。

## 六、进一步提速与并行边界

### 6.1 首选双Terra模式

后续章节首选“双Terra内容通道＋一次Sol审计”：Terra A处理1000题题面、解析和题目包，Terra B处理教材正文核心、教材例题/习题和大纲。主Agent同时执行不含语义识别的本地工作：页码/书签盘点、范围清单、空记录初始化、KaTeX输入预检、ID/关系检查器和报告骨架。

双Terra启动前必须做轻量预检：确认两个通道范围不重叠、输出目录隔离、当前服务允许两个并发 Terra high，且没有立即出现429。若预检或运行中出现429、写入冲突、来源不一致或游标异常，立即停止双通道，保留已完成批次并切回单Terra串行模式。

### 6.2 批次大小

页面密度正常时，题面或教材核心按8–15页一批，解析页按4–8页一批；公式密集、跨页或图像复杂时自动缩小批次。每批只检查写入、计数和游标，避免逐页重复验收。

### 6.3 双Terra通道边界

双Terra是首选执行模式，但必须将一个章节拆为两个互不重叠的通道：

- 通道A：1000题题面、解析和题目包；
- 通道B：教材正文核心、教材例题/习题和大纲。

两个通道必须使用隔离的 staging 和临时输出目录，不得同时写同一文件或同一页；两者全部完成并由主Agent合并冻结后才启动一次 Sol high。出现429、版本冲突或游标异常时，立即停止双通道并从各自游标切回串行模式。不得把同一页拆给两个Agent，也不得并行启动Sol。

### 6.4 不能牺牲的内容门槛

并行只改变执行时序，不改变固定分母、题干/解析文字化、教材核心覆盖、题型与本题评述、KaTeX全量编译和高风险审计要求。不得为了60分钟目标减少题目、跳过官方解析或把低风险题默认为正确。
